# --- Етап 1: Використання базового образу ---
# Використовуємо офіційний, легкий образ Nginx на базі Alpine Linux.
# Це робить кінцевий розмір контейнера дуже малим.
FROM nginx:1.27-alpine-slim

# --- Етап 2: Копіювання файлів сайту ---
# Копіюємо ваш єдиний HTML-файл у стандартну директорію Nginx,
# з якої він роздає статичний контент.
COPY index.html /usr/share/nginx/html/

# --- Етап 3: Відкриття порту ---
# Інформуємо Docker, що контейнер буде слухати на порту 80.
# Nginx за замовчуванням працює на цьому порту.
EXPOSE 80

# --- Етап 4: Запуск сервера ---
# Ця команда вже вбудована в базовий образ Nginx,
# тому її не обов'язково додавати, але для ясності:
# вона запускає Nginx у режимі, що не дозволяє контейнеру завершити роботу.
CMD ["nginx", "-g", "daemon off;"]
